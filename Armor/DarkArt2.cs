using Microsoft.Xna.Framework;
using System.Collections.Generic;
using System.Linq;
using Terraria;
using Terraria.ID;
using Terraria.ModLoader;
using Microsoft.Xna.Framework.Graphics;
using clericclass.ClericBase;
using clericclass.Weapons.Templates;

namespace clericclass.Armor
{
    /*
     * do this later, dark artist armor but instead of magic it has necrotic because why not
     * made at a crystal ball with original armor
    [AutoloadEquip(EquipType.Head)]
    class DarkArt2Head : ModItem
    {
        public override string Texture => "Terraria/Item_" + ItemID.ApprenticeAltHead;

        public override void SetStaticDefaults()
        {
            base.SetStaticDefaults();
        }
    }
    */
}
